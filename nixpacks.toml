[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['cd backend && npm ci --only=production']

[phases.build]
cmds = ['echo "No build step required"']

[start]
cmd = 'cd backend && node server.js'
